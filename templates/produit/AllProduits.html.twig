{% extends 'layout.html.twig' %} {% block title %}{{ parent() }}Tous les produits{% endblock %}
{% block content %}
<div class="row mt-5 justify-content-center text-center">
  <h1 class="text-success">Liste des produits </h1>
  <div class="container">
    <br /><br />
    <div class="row">
      {% for produit in allProduits %}
      <div class="col col-md-3 mt-3">
        <div class="card" style="width: 100%">
          <div class="card-body">
            <h5 class="card-title">{{ produit.titre }}</h5>
              <img width="100%" src="{{ asset('uploads/' ~ produit.photo) }}">
            <h6>Titre : {{ produit.titre}}</h6>
            <h6>Couleur : {{ produit.couleur}}</h6>
            <h6>Taille : {{ produit.taille}}</h6>
            <h6>Stock : {{ produit.stock}}</h6>
            <h6 class='text-danger'>Prix : {{ produit.prix}}</h6>
            <p class="card-text">{{ produit.description | slice(0,150)}} ...</p>
            <button class="btn btn-success"><a href="">Acheter</a></button>
          </div>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</div>
{% endblock %}